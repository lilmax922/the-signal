# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- [x] Design system configuration
- [x] Layout components
- [x] Auth
- [x] Drizzle ORM setup & initial migration
- [x] Refinery pipeline (ingest → extract → de-noise → slug → mirror)
- [x] Signal persistence (DB write stage of pipeline)
- [x] Refinery logging & return refactor (structured logger + typed return)
- [x] Cursor pagination indexes (composite + per-category partials; drop redundant single-col)
- [x] Signal API foundation (Zod schemas, limits constants, Drizzle relations)
- [x] Signal API routes (GET /api/signals feed + GET /api/signals/[slug] detail)
- [x] Feed page wiring (parent route + signal-feed + signal-card, no Pinia)
- [x] Signal detail overlay (query-based overlay: signal-detail-overlay.vue + USlideover/UDrawer + signal-detail)
- [x] DB connection update for serverless (transaction pooler + direct connection)
- [x] Search API wiring (search command palette → live API + GIN trigram indexes)

## Current Goal

- Core feature implementation per `project-overview.md`

## Completed

- **Temp: Auth Disabled for Development** (dev-only, see "Re-enable checklist" below): All authentication surfaces have been commented out to remove the login wall during development. Eight files touched, all with a consistent `DEV-AUTH-DISABLED:` marker so the re-enable pass is greppable. `@nuxtjs/supabase` module is **still loaded** in `nuxt.config.ts` and the env vars (`NUXT_PUBLIC_SUPABASE_URL` / `_KEY`, `SUPABASE_SERVICE_ROLE_KEY`) are **still validated** by `shared/env.ts` — only the call sites are disabled. The Supabase storage client used by the refinery pipeline (`trigger/utils/mirror-image.ts` → `createStorageClient()`) is independent of the Nuxt module, so the image upload pipeline is **unaffected**. Files changed:
  - `app/middleware/auth.global.ts` — entire middleware body wrapped in `/* … */`. Global route guard is a no-op: anyone can hit any URL.
  - `app/pages/login.vue` — `<script setup>` body (`useSupabaseClient` + `signInWithOAuth` + `providers` + `features`) wrapped in `/* … */`. `<template>` replaced with a static dev notice (`登入功能已暫時停用` + a "返回首頁" link). `definePageMeta({ layout: false })` kept outside the block.
  - `app/pages/confirm.vue` — `useSupabaseUser` watch logic wrapped in `/* … */`. `<template>` replaced with a static dev notice. `<style scoped>` char-bounce keyframes removed (no longer needed).
  - `app/components/app/header.vue` — `useSupabaseUser` / `useSupabaseClient` / `avatarUrl` / `userEmail` / `userMenuItems` wrapped in `/* … */`. The `<UDropdownMenu>` block in `<template>` replaced with a single disabled `<UButton variant="ghost" color="neutral" disabled aria-label="登入功能已停用">` + static `<UAvatar icon="i-lucide:user">` placeholder, so the header right-side slot stays the same width and the layout doesn't shift. `useScrollCollapse` left untouched.
  - `server/api/signals/index.get.ts` — `import { serverSupabaseUser }` + the `serverSupabaseUser(event)` + 401-throw block wrapped in `/* … */`. The Zod query parse + `findSignals` call + the 400/500 error paths are unchanged.
  - `server/api/signals/[slug].get.ts` — same treatment as the index route. The slug validation + `findSignalBySlug` + Zod re-parse + 400/404/500 error paths are unchanged.
  - `app/pages/[[category]].vue` — the `onResponseError({ response }) { if (response.status === 401) navigateTo('/login') }` inside the `useFetch` options wrapped in `/* … */`. The other options (`key`, `query`) and the `data`/`status`/`refresh` destructuring are unchanged.
  - `app/pages/[[category]]/[slug].vue` — same `onResponseError` 401 block wrapped in `/* … */`. The `immediate: false` + reactive `key` + `watch(slugIsValid, …)` flow is unchanged.
  - **Re-enable checklist** (one-liner): `rg -n "DEV-AUTH-DISABLED" app/ server/` lists every disabled block. For each hit, (a) remove the surrounding `/* … */` (or the single-line `// DEV-AUTH-DISABLED: …` marker) and (b) restore any code that was inside it. The disabled blocks are fully self-contained — no cross-file coupling was added during the disable pass, so the re-enable pass is mechanical. **This change is temporary — every API endpoint is currently publicly accessible, and `/api/signals` + `/api/signals/[slug]` must be re-protected before any production deploy.** `pnpm lint` / `pnpm typecheck` / `pnpm build` all pass; only the two pre-existing `no-console` warnings in `server/tasks/rss-ingestion.ts` and `server/utils/fetch-rss.ts` remain.

- **Design System** (`01-design-system.md`): Dark theme, OKLCH brand color, bilingual fonts, typography system
- **Layout** (`02-layout.md`): Desktop/mobile responsive layout, header, CategoryFilter, MobileBottomNavbar, scroll-collapse
- **Auth** (`03-auth.md`): Login page with OAuth (Google/GitHub) via `@nuxtjs/supabase`
- **Drizzle ORM** (`04-setup-drizzle.md` + `database-schema.md`): Supabase dual connection (transaction pooler for runtime, direct for migrations), `server/database/` schema with `pgEnum` for `category`, `slug` column, `prepare: false`, `casing: 'snake_case'`, clean initial migration applied
- **RSS Nitro Task** (`06-rss-nitro-task.md`): `server/database/queries/signal.ts` — `findSignalByGuid(guid)` query with Drizzle ORM relational query API; `shared/validators/rss.ts` — added `refineryPayloadSchema` and `RefineryPayload` type; `server/tasks/rss-ingestion.ts` — Nitro task with `name: "rss-ingestion"`, fetches Yahoo RSS feeds for finance/tech/world categories, de-duplicates by guid, validates against `refineryPayloadSchema`, limits to 5 new articles per category, placeholder for Trigger.dev pipeline; `nuxt.config.ts` scheduled tasks for `0 8,20 * * *` (08:00 and 20:00 daily)
- **Trigger.dev Setup** (`07-trigger-dev.md`): Installed `@trigger.dev/sdk` (v4.4.6), created `trigger.config.ts` with node-22 runtime, retries disabled in dev; created `trigger/` directory with `example.ts` (example task) and `refinery.ts` (schemaTask for article processing with Zod validation)
- **Extract Detail Content** (`07-extract-detail-content.md`): Installed `@extractus/article-extractor` (v8.1.0) and `undici`; created `trigger/refinery-agent.ts` — schemaTask that validates payloads against `refineryPayloadSchema`, extracts article content via `@extractus/article-extractor`, strips HTML tags, and logs extracted content; updated `server/tasks/rss-ingestion.ts` to trigger `refinery-agent` task with validated payload data; created `shared/utils/extractor.ts` using `undici`'s `setGlobalDispatcher` with `maxHeaderSize: 32768` — needed because Trigger.dev tasks run outside the Nuxt Nitro context where `$fetch` auto-imports aren't available, and to bypass Yahoo Finance's `HeadersOverflowError` (16KB default limit); uses browser User-Agent headers to bypass anti-bot blocking
- **Slug Generation and Image Mirroring** (`09-slug-generation-and-image-mirroring.md`): Added `SUPABASE_SERVICE_ROLE_KEY` to `shared/env.ts` (and `.env.example`); added `@supabase/supabase-js@^2.106.1` to `package.json` (transitive from `@nuxtjs/supabase`, declared explicitly because pnpm's strict mode blocks importing undeclared packages); created `shared/utils/create-storage-client.ts` exporting a memoized `createStorageClient()` Supabase client configured with the service role key and session persistence disabled (suitable for backend-only use); created `trigger/utils/slug.ts` with `generateSlug(title, publishedAt)` that lowercases, normalises (NFKD + diacritic strip), collapses non-alphanumerics to single hyphens, and appends `YYYYMMDD` from UTC components; created `trigger/utils/mirror-image.ts` with `mirrorImage(imageUrl)` that downloads via `undici` (15s timeout, browser UA), re-encodes to **WebP via `sharp`** (quality 80, max width 1280 px, with a quality-65 retry when the result exceeds a 95 KB soft target — the smaller of the two buffers is uploaded), uploads to the `signal-images` bucket using a `crypto.randomUUID()` filename, and returns the public URL; `sharp` failures fall back to uploading the original bytes; updated `trigger/refinery-agent.ts` to compute `slug` after LLM success, attempt mirroring (logging but not aborting on failure per architecture invariant), and return `{ slug, mirroredImageUrl, …existing }`; updated `architecture.md` and `database-schema.md` to use `YYYYMMDD` (was `YYYY-MM-DD`) for consistency with the spec
- **Store Signal Data** (`10-store-signal-data.md`): Added `drizzle-zod@0.8.3` (compatible with `drizzle-orm@0.45.2` and `zod@4.4.3`); appended Zod **insert schemas** to the existing table files — `InsertSignal` in `server/database/schema/signal.ts` (with `summaryEn`/`summaryZh` length-3 refinements to mirror the LLM contract, and `.omit({ id, createdAt, updatedAt })`), `InsertTag` in `server/database/schema/tag.ts` (same `.omit`), and `InsertSignalTag` in `server/database/schema/signal-tag.ts` (no fields to omit — only `signalId` + `tagId`); inferred `InsertSignal` / `InsertTag` / `InsertSignalTag` types via `z.infer`; created three query helpers in `server/database/queries/` — `insertSignal`, **`insertTag`** (uses `onConflictDoUpdate({ target: tag.name, set: { name: sql\`excluded.name\` } })` internally so the same row is returned whether the tag already existed or was just inserted — no `SELECT` fallback required; the function is named `insertTag` for naming consistency with the other helpers even though the underlying SQL is an upsert), and `insertSignalTag`; all three validate the incoming payload via `safeParse(...)` of their respective Zod insert schema and throw an explicit error when the insert returns no row (no `!` non-null assertion operator); all three accept an optional `DbClient` (derived as `typeof db | Parameters<Parameters<typeof db.transaction>[0]>[0]`) so they participate transparently in a `db.transaction(async (tx) => …)` boundary; marked `categoryEnum` in `signal.ts` as module-private (no longer re-exported from `schema/index.ts`) so the schema namespace satisfies the `TablesRelationalConfig` constraint that `PostgresJsTransaction` requires; updated `trigger/refinery-agent.ts` to call the three helpers inside a `db.transaction` block — `insertSignal` → loop `insertTag` + `insertSignalTag` per `llmOutput.tags` entry — and return `{ …existing, persisted: { insertedSignal, insertedTags, insertedSignalTags } }` so the stored rows are visible in the trigger.dev dashboard for manual verification


- **Refinery Logging & Return Refactor** (`11-refinery-logging-return.md`): Restructured `trigger/refinery-agent.ts` into a single linear `run()` with 5 commented step blocks (extract → LLM → slug → mirror → persist) — no helper functions or file splits. Added `LOG` constants object at module top (`refinery.start`, `refinery.extract.ok`, `refinery.refine.ok`, `refinery.slug.ok`, `refinery.mirror.{ok,skipped,err}`, `refinery.persist.ok`, `refinery.complete`) so every log call uses a stable, greppable message key instead of string interpolation; `log(stage)` arrow at the top of `run()` builds the shared `{ pipelineRunId, guid, stage }` property bag and gets spread into every call so `guid`/`pipelineRunId` are never retyped. Added `RefineryError` class carrying a typed `code` (`EXTRACT_FAILED` | `QUALITY_GATE` | `LLM_FAILED` | `SLUG_FAILED` | `PERSIST_FAILED`) so the dashboard error panel groups failures by reason instead of by raw `ZodError` / `TypeError`; each stage wraps its single async call in a tiny `try { … } catch (err) { throw new RefineryError(CODE, …, err) }` block — the half-log-then-rethrow pattern from the previous version is gone. Reintroduced the 200-character **quality gate** in the extract step that was dropped from the intermediate file — too-short content now throws `RefineryError('QUALITY_GATE', …)` instead of being passed to the LLM (which would have produced garbage). Demoted the verbose `llmOutput` info log to summary counts only (`titleLength`, `summaryPointCount`, `tagCount`) — full article bodies never reach the run log timeline. Image mirroring remains the single soft-fail path per `architecture.md` (`logger.warn` + continue with `null`); other stages throw. Slimmed the run output to a typed `RefineryResult = { status: 'success', slug, signalId, tagCount, imageMirrored, durationMs }` — input fields, `timestamp`, the full `llmOutput`, and the full `persisted` rows are no longer echoed back (they're either in the input or in the DB). Each `…_OK` log is the single source of success visibility for that stage; `PERSIST_OK` and `COMPLETE` carry the slim `RefineryResult` payload as properties.

- **Cursor Pagination Indexes** (`11-cursor-pagination-indexes.md`): Replaced the single-column `idx_signal_published_at` with the composite `idx_signal_published_at_id` on `(published_at DESC, id DESC)` in `server/database/schema/signal.ts` — anchors the `WHERE (published_at, id) < (?, ?)` row-tuple comparison that powers cursor pagination; the `id` half is the tiebreaker when two rows share the same `published_at` (chosen over `slug` because `id` is a fixed-size immutable UUID and the canonical internal handle, while `slug` is a variable-length public URL identifier). Added three per-category partial indexes on the same composite — `idx_signal_finance_published_at_id`, `idx_signal_tech_published_at_id`, `idx_signal_world_published_at_id` — so filtered feed queries hit small category-scoped indexes instead of scanning the full composite. Retained the legacy `idx_signal_category_published` for non-cursor category-only queries (admin views, back-office tasks). The auto-emitted `pgEnum` cast in the partial `WHERE` clause (`category = 'finance'::category`) comes from Drizzle reading the enum type. New migration `0001_harsh_onslaught.sql` applied cleanly; verified the five indexes via a one-off `pg_indexes` query (script removed after verification). `pnpm lint`, `pnpm typecheck`, and `pnpm build` all pass.
- **Feed Page Wiring** (`13-feed-page-wiring.md`): Built the consumer-side feed UI on top of the existing `GET /api/signals` endpoint — **deliberately without Pinia or a custom composable**. Deleted `app/pages/index.vue` (the placeholder) and replaced it with the optional-dynamic parent route `app/pages/[[category]].vue` (per `code-standards.md:62-66`); the parent component is mounted exactly once per browser session (invariant #9) and naturally hosts the feed's reactive state, so no external store is needed. The page validates `route.params.category` against `categorySchema` and throws `createError({ statusCode: 404 })` for invalid values per `ui-context.md:81`; the validated value flows into a `computed<Category | undefined>()` named `category`. Initial fetch uses `useFetch<FeedResponse>('/api/signals', { key: computed(() => 'signal-feed-' + (category.value ?? 'all')), query: computed(() => ({ category: category.value })) })` — both the `key` and the `query` are reactive, so navigating from `/` to `/finance` automatically refetches with a fresh cache key and the per-category partial index is hit server-side. Local `items` / `cursor` / `hasMore` / `isLoadingMore` refs are seeded from `data` via a `watch(data, …, { immediate: true })`. `loadMore()` is guarded by `hasMore && !isLoadingMore && cursor`, calls plain `$fetch<FeedResponse>('/api/signals', { query: { category, cursor } })` (per the Nuxt docs, `$fetch` is the correct tool for client-side event-driven fetches — not initial data), and appends into `items`. The 401 path is handled in `useFetch`'s `onResponseError` by calling `navigateTo('/login')` (defence-in-depth on top of `app/middleware/auth.global.ts`). `app/components/signal/signal-card.vue` renders the anatomy from `ui-context.md:144-151` — image (skipped when `imageUrl` is null), category badge + `publishedAt` (`Intl.DateTimeFormat('zh-TW', { dateStyle: 'medium', timeZone: 'UTC' })` wrapped in `<time :datetime=…>`), `text-xl font-medium leading-relaxed` title (Traditional Chinese), three numbered mono-indexed summary points with `tracking-wide` for ideogram legibility, and entity tag chips (`text-xs font-mono uppercase tracking-wide`, no background fill, row only rendered when `signal.tags.length > 0`); zero click handlers / no `router.push` / no `<NuxtLink>` — that wiring is deferred to the detail-overlay spec. `app/components/signal/signal-feed.vue` owns the `grid grid-cols-1 md:grid-cols-2 gap-6 max-w-7xl mx-auto px-4 md:px-8 lg:px-12` container per `ui-context.md:130-142`, plus four mutually exclusive render paths: (1) initial-load `animate-pulse` skeleton (3 placeholder cards with neutral `bg-elevated` blocks matching the real card geometry), (2) error state with a `<UButton variant="outline" color="neutral">重試</UButton>` that emits `retry` (parent calls `refresh()`), shown only when `error && items.length === 0` so subsequent `loadMore` failures don't wipe the rendered list, (3) empty state, (4) the items grid. Infinite scroll uses a `h-1` sentinel rendered only when `hasMore` plus VueUse's `useIntersectionObserver` with `rootMargin: '200px'` (explicitly imported from `@vueuse/core` per the `use-scroll-collapse.ts` pattern — auto-import isn't enabled for vueuse composables in this Nuxt setup, learned via the `useIntersectionObserver` typecheck error). `pnpm lint` (0 errors, 2 pre-existing console-statements warnings in `server/tasks/rss-ingestion.ts` and `server/utils/fetch-rss.ts` unrelated to this spec), `pnpm typecheck`, and `pnpm build` all pass; build emitted `default-Uqn1_dvG.mjs` (216 kB / 23.7 kB gzip) for the layout chunk containing the new page.
- **Feed Page Bug Fixes** (post-#13 follow-up, round 1): Fixed two issues reported after the #13 merge. (1) **Hard refresh on `/` threw `404 invalid category`**: the `category` computed in `app/pages/[[category]].vue:9` was treating `route.params.category` as `undefined` on `/`, but Nuxt 4's optional-dynamic route (`[[category]]`) actually resolves to an empty string `''` on the root path — `Zod.enum` rejects empty strings, so the computed threw a 404 on every root refresh. Changed the early return to `if (c === undefined || c === '') return undefined` so both `undefined` and `''` map to "no category". The `typeof c !== 'string'` guard below still catches the (defensive) array case for any future nested-route refactor. (2) **`/confirm` page stuck after OAuth callback**: the existing watch (`app/pages/confirm.vue:8`) was correct in logic but missing `return` on `navigateTo('/')` — the official `@nuxtjs/supabase` demo at `nuxt-modules/supabase@bc2d1413/demo/pages/confirm.vue` uses the exact same pattern with the `return` keyword, and the missing return was the only diff. Root cause was actually more interesting: even with the `return` fix, the redirect still wouldn't complete because the destination `/` page was throwing the category 404 from bug (1) during its setup, which cancelled the navigation and left `/confirm` rendered. Bug (1) is the structural fix; the `return` change in `/confirm` is belt-and-suspenders to match the upstream demo exactly. `pnpm lint` / `pnpm typecheck` / `pnpm build` all pass; only the two pre-existing `no-console` warnings remain.
- **Feed Page Bug Fixes** (post-#13 follow-up, round 2): Fixed two more issues. (1) **`loadMore` infinite scroll returned `500 internal error`**: the cursor predicate in `server/database/queries/signal.ts:86-91` used raw `sql` tag interpolation for the `publishedAt` Date value (`sql\`${signal.publishedAt} < ${cursorRow.publishedAt}\``). When Drizzle's `sql` tag encounters a raw value (not a column reference), it wraps it in a `Param` without an encoder — the `postgres` driver then rejects it with `ERR_INVALID_ARG_TYPE: "string" argument must be of type string or an instance of Buffer or ArrayBuffer. Received an instance of Date` (surfaced as `Failed query` → caught by the route's catch block → rethrown as 500). The fix: replaced the three raw `sql` tags with Drizzle's `lt` / `eq` operators, which wrap the value via `bindIfParam(value, column)` so the Date is encoded against the column's `timestamp` encoder before reaching the driver. Imported `lt` from `drizzle-orm` and dropped the now-unused `sql` import. **Verified end-to-end** by calling `findSignals` with the exact failing cursor (`MjAyNi0wNi0wNFQwMDowMDowMC4wMDBafGU0NDAxOWY1LTA0YjItNDAxMC1hODg4LTg2MjRlMmYzNWUxMA==` → `2026-06-04T00:00:00.000Z|e44019f5-04b2-4010-a888-8624e2f35e10`) — now returns 5 items with `hasMore: false, nextCursor: null` instead of 500. (2) **CategoryFilter clicks didn't update the URL or the feed**: `app/components/category-filter.vue` owned a local `selected` ref whose click handler only mutated the ref (no `navigateTo`). Refactored the component into a route-aware rail: added a `categories` array `[{ label, slug }]` mapping the 4 UI labels to API slugs (`全部 → null`, `科技 → tech`, `股市 → finance`, `國際 → world` — the `國際` label pre-dates the API's 3-category enum and maps to the closest available slug; flagged as a follow-up for when the taxonomy expands); derived `currentCategory` from `route.params.category` (with the same `undefined || ''` guard as the feed page); derived `selected` as a `computed` that looks up the matching label by slug; the click handler now calls `navigateTo('/')` for `null` or `navigateTo('/${slug}')` otherwise. The feed page's `category` computed + `useFetch` reactive `key` already refetch on route change, so the wiring is end-to-end. **Verified** by temporarily adding a test page + auth bypass: clicking 科技 / 股市 / 國際 on the rail all triggered `navigateTo` to the correct path (the auth middleware then redirected to `/login` since no user was set — expected behavior for the unauthenticated test). `pnpm lint` / `pnpm typecheck` / `pnpm build` all pass; only the two pre-existing `no-console` warnings remain.
- **Feed Page Design Pass** (post-#13 follow-up, round 3 — responsive grid + end-of-feed marker + Nuxt UI migration): Three small design/UX polish items requested after the round-2 fixes. (1) **Grid breakpoint expansion to 1/2/3 columns**: changed the feed grid from `grid grid-cols-1 md:grid-cols-2` (mobile 1, tablet+desktop 2) to `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3` (mobile 1, tablet 2, desktop 3). The three-column desktop layout is the dominant reading surface; tablet is transitional. The `gap-y-10` vertical gap and `pb-16` bottom padding were also added so the end-of-feed marker and the mobile bottom navbar do not crash into the last card row. (2) **End-of-feed marker**: added a quiet visual terminus that renders when `!hasMore && items.length > 0 && !isLoadingMore` — two hairline dividers flanking a `<UIcon name="i-lucide-check" />`, with a `text-xs font-mono uppercase tracking-wide text-muted` label "已顯示全部訊號". The marker spans all columns (`col-span-1 md:col-span-2 xl:col-span-3`) and sits inside the grid as the last row, so it respects the responsive layout at every breakpoint. Aesthetic rationale: a "no results" empty state would be misleading (there are results — the user reached the end); a hard "end of list" message would be jarring. The hairline+check terminus is a calm, terminal signal that fits the "Refined Data Terminal" direction in `ui-context.md:1-5`. (3) **Nuxt UI component migration**: replaced the manual loading skeleton (3 raw `<div class="animate-pulse bg-elevated/40 rounded-2xl">` blocks) with `<USkeleton class="h-96 rounded-2xl" />` (count bumped from 3 to 6 so the skeleton fills 2 rows on the 3-column desktop grid); replaced the manual error block (centred text + outline button) with `<UEmpty icon="i-lucide-alert-circle" title="載入失敗" description="…" :actions="[{ label: '重試', icon: 'i-lucide-refresh-cw', onClick: () => emit('retry') }]" />`; replaced the manual empty block (centred muted text) with `<UEmpty variant="naked" icon="i-lucide-inbox" title="目前沒有任何訊號" description="…" />`; replaced the text-only "載入中" loading-more indicator with a spinning `<UIcon name="i-lucide-loader-circle" class="size-4 animate-spin" />` + label. **Verified visually** at all three breakpoints (375 / 768 / 1280) against the real Supabase dataset — 3-column desktop grid is balanced, 2-column tablet is clean, 1-column mobile cards are full-width, and the end-of-feed marker has comfortable spacing above the mobile bottom navbar at all sizes. `pnpm lint` / `pnpm typecheck` / `pnpm build` all pass; only the two pre-existing `no-console` warnings remain. **Docs updated**: `context/feature-specs/13-feed-page-wiring.md:44-51` (breakpoints + skeleton count + Nuxt UI components), `context/ui-context.md:130-142, 167-168` (renamed "Two-Column Card Stream" → "Three-Column Card Stream" with new ASCII diagram, grid spec updated, Intelligence Pane closing-restore wording updated), and `context/project-overview.md:36` (three-column grid with responsive note).
- **Signal API** (`12-signal-api.md`): Built the read-side Nitro API. Created `shared/constants/limits.ts` with `FEED_PAGE_SIZE = 10` + `FEED_PAGE_SIZE_MAX = 50`. Expanded `shared/validators/signal.ts` — renamed `CategorySchema` → `categorySchema` (camelCase, per `code-standards.md`) and updated both consumers (`shared/validators/rss.ts` and `server/utils/fetch-rss.ts`); added `signalSchema` (full response, 3-item `summaryEn`/`summaryZh` via a shared `summarySchema` helper, internal fields `guid` / `createdAt` / `updatedAt` / `pipelineRunId` excluded), `signalFeedSchema` (derived from `signalSchema` via `.omit({ contentEn, contentZh, summaryEn, sourceUrl })` — no field duplication), `signalQuerySchema` (with `FEED_PAGE_SIZE` + `FEED_PAGE_SIZE_MAX` wired in), and `feedResponseSchema` envelope; inferred `Signal` / `SignalFeed` / `FeedResponse` / `SignalQuery` types. Added Drizzle `relations()` to all three schema files — `signalRelations({ tags: many(signalTag) })`, `tagRelations({ signals: many(signalTag) })`, `signalTagRelations({ signal: one(signal, …), tag: one(tag, …) })` with explicit `fields` / `references` on the junction so the relational query API resolves `signal → signal_tag → tag` in one round-trip. Added `findSignals({ category?, cursor?, limit })` to `server/database/queries/signal.ts` — over-fetches by 1 to derive `hasMore` without a second `COUNT(*)`, uses the row-tuple predicate `WHERE (published_at, id) < (?, ?)` (or `AND category = ?` when filtered) so the existing composite + per-category partial indexes are used, encodes the cursor as `base64({publishedAt-iso}|{id})` and re-validates the final response via `feedResponseSchema.safeParse` as defence-in-depth. Added `findSignalBySlug(slug)` returning the full bilingual signal + tags or `null`. Created two route handlers: `server/api/signals/index.get.ts` (auth → `signalQuerySchema.safeParse` → `findSignals`; 401/400/500 paths; cursor decode failures surface as 400) and `server/api/signals/[slug].get.ts` (auth → `getRouterParam` → `findSignalBySlug`; 401/400/404/500 paths; final `signalSchema.safeParse` for contract drift detection). Both use `serverSupabaseUser` from `#supabase/server`. `pnpm lint --fix`, `pnpm typecheck`, and `pnpm build` all pass — Nitro bundles the two new routes as `routes/api/index.get.mjs` + `routes/api/signals/_slug_.get.mjs`.

- **Signal Detail overlay** (`14-signal-detail-overlay.md`): Implemented the consumer-side detail view on top of `#12` + `#13`. Initial implementation used nested-route master-detail (`/{category?}/{slug}` child route + USlideover). Restructured (post-#14 round 3) to query-based overlay: card clicks push `?signal=slug`, direct links use `/signal/{slug}` (SSR page for crawlers). **Final architecture**: `signal-detail-overlay.vue` owns the full overlay lifecycle (responsive USlideover/UDrawer, fetch, close logic); `[[category]].vue` reads the query and passes the slug — overlay is a feed-page concern, not global. Files: created `signal/[slug].vue` (SSR page), `signal-detail-overlay.vue` (self-contained overlay); modified `[[category]].vue` (feed host + overlay host), `signal-card.vue` (query navigation), `category-filter.vue` (cleanup); deleted `[[category]]/[slug].vue`, `signal-detail-modal.vue`. **Three files changed in `app/components/signal/`**: `signal-card.vue` now accepts an optional `category?: Category` prop and wraps its body in `<NuxtLink :to="`/${category ?? ''}${category ? '/' : ''}${signal.slug}`">` (with a `focus-visible:ring-2 focus-visible:ring-brand-500 rounded-2xl` outline on the link wrapper so the whole card stays keyboard-navigable); `signal-feed.vue` accepts and forwards `category` to every `<SignalCard>`; new `signal-detail.vue` (props `{ signal: Signal }`, emits `close: []`) is a pure rendering component with **no** viewport logic, focus management, or overlay chrome — anatomy is `header` (category badge + `<time :datetime="signal.publishedAt">` formatted with `Intl.DateTimeFormat('zh-TW', { dateStyle: 'medium', timeZone: 'UTC' })` + a `<UButton icon="i-lucide-x" variant="ghost" color="neutral" size="sm" aria-label="關閉" @click="emit('close')">` close button), `aspect-video w-full rounded-2xl object-cover` hero image (skipped when `signal.imageUrl` is null, `loading="eager"` so the LCP image is the first paint), `text-2xl xl:text-3xl font-medium leading-relaxed text-highlighted` title rendering `signal.titleZh`, `space-y-2` 3-point summary with the two-digit mono index from the card, monospace `text-xs uppercase tracking-wide` entity tag row (skipped when `signal.tags.length === 0`), `text-base font-light leading-relaxed tracking-wide whitespace-pre-line text-default` body rendering `signal.contentZh` (per `project-overview.md:29` the UI strictly renders `contentZh`), `<USeparator />`, and finally a `<a :href="signal.sourceUrl" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 text-sm font-mono text-muted hover:text-highlighted transition-colors">` with `i-lucide-external-link` and `new URL(signal.sourceUrl).hostname`. **Two files in `app/pages/`**: new `[[category]]/[slug].vue` is the child route — reads `route.params.slug` (with a `typeof === 'string'` guard for the future nested-route case), calls `useFetch<Signal>(`/api/signals/${slug.value}`, { key: () => `signal-detail-${slug.value}`, onResponseError({ response }) { if (response.status === 401) navigateTo('/login') } })` — `key` is a function returning a fresh template string so each slug refetches against its own payload key — derives `isLoading` / `hasError` from `status` and renders `<SignalDetail v-else-if="signal" :signal="signal" @close="emit('close')" />` with plain `載入中…` / `載入失敗` placeholder strings (polished states deferred to `#15` per spec). Modified `[[category]].vue` — added `closeDetail()` (reads `route.params.category`, calls `router.push(cat ? `/${cat}` : '/')` — `router.push` is the only navigation primitive, **no** `router.replace` anywhere per the spec's "Check When Done" list), a writable `isOpen` computed (getter `() => !!route.params.slug`; setter calls `closeDetail()` on `false` so USlideover's internal `v-model:open` write closes the overlay via the URL), and a `<USlideover v-model:open="isOpen" side="right" :close="false" :ui="{ header: 'hidden', body: 'p-0 sm:p-0' }">` placed after `<SignalFeed />` with `<NuxtPage />` as the `#body` slot — USlideover is built on Reka UI Dialog so body scroll lock, focus trap, focus return, Esc, and backdrop click are handled internally; `:close="false"` + the `header: 'hidden'` ui override hide the default header so `signal-detail.vue` owns its own header inside the body. The parent forwards `category` to `<SignalFeed :category="category" ... />` so the card-link URL preserves the active filter. **Why no SSR viewport plugin (yet)**: deferred to `#15` along with the responsive `< lg` UDrawer variant. **v1 trade-off**: USlideover's `max-w-md` (from `.nuxt/ui/slideover.ts:26`) is wider than the right-side pane sketched in `ui-context.md:170` (which suggested `p-8` internal padding and a single-column feed to the left); the v1 visual is "slideover that occludes the right edge of the feed" rather than "pane that narrows the feed to one column" — acceptable for the first end-to-end cut because the spec explicitly says "no narrowing, no layout reflow" in the Objective block. `pnpm lint` / `pnpm typecheck` / `pnpm build` all pass; build output confirms two new chunks (`_slug_-OCYLORrg.mjs` for the child route, `Separator-D8SMMJR4.mjs` for the USeparator in the detail) and the pre-existing `_category_-aH-I7UDQ.mjs` now hosts the USlideover wrapper.
- **Signal Detail overlay Bug Fixes** (post-#14 follow-up): Three regressions reported after the #14 merge, all addressed. (1) **Close button unresponsive**: `<NuxtPage />` does not forward `$emit` from the routed child component, so the parent's `<USlideover>` never received the `close` event the X button was emitting. Moved the 3-line `closeDetail()` logic into the child `app/pages/[[category]]/[slug].vue` itself — it reads `route.params.category` and calls `router.push(category ? `/${category}` : '/')` directly. The parent keeps its own `closeDetail()` for `USlideover`'s `set: !value` path, but the X button now works without prop drilling. (2) **Feed jumped to top when opening or closing the overlay, and focus snapped to the slideover body on open / back to the card on close**: added `app/router.options.ts` with a custom `scrollBehavior` — `savedPosition` for back/forward, `false` when `parentPath(to.path) === parentPath(from.path)` (same parent route, e.g. `/finance` → `/finance/abc-20260605`), `{ top: 0, left: 0 }` otherwise. The Nuxt 4 `false` return suppresses both the scroll-to-top and the focus reset Reka UI ties to navigation. Also passed `:content="{ onOpenAutoFocus: (e) => e.preventDefault(), onCloseAutoFocus: (e) => e.preventDefault() }"` to `<USlideover>` to disable Reka UI's auto-focus management on the dialog body (open) and the trigger element (close) — user explicitly requested NO ESC→focus-restore-to-card feature, so the dialog is purely visual. (3) **Clicking a category in the filter opened the slideover with `載入失敗`**: the optional-dynamic + child-route combo (`app/pages/[[category]]/[slug].vue` → both `/{slug}` and `/{category?}/{slug}`) routes single-segment `/finance` to the child, which then tried to fetch `/api/signals/finance` and got a 404. Fixed with a category-slug guard in two places: the child's `useFetch` uses `immediate: false` + a `watch(slugIsValid, …)` (where `slugIsValid = slug && !categorySchema.safeParse(slug).success`) that calls `refresh()` only when the slug is a real signal, otherwise resets `signal.value = undefined`; the parent's `isOpen` computed now also returns `false` when `categorySchema.safeParse(slug).success` is true, so the slideover never opens for a category name. **Why a guard instead of a 404 redirect**: Nuxt 4's `definePageMeta({ validate })` return type is `boolean | Partial<NuxtError> | Promise<…>` — it does NOT accept `router.push` / `navigateTo` returns, so redirecting inside `validate` would require `as never` casts. The guard is structurally simpler and matches the spec's "no `router.replace`" rule (no navigation happens for category slugs at all). `pnpm lint` / `pnpm typecheck` / `pnpm build` all pass; only the two pre-existing `no-console` warnings remain.
- **Category Filter Active State Desync** (post-#14 follow-up, round 2): Clicking a category in the filter updated the URL but the active-tab highlight stayed on `全部`, and the feed showed the wrong category. **Root cause**: the optional-dynamic + child-route combo (`app/pages/[[category]].vue` + `app/pages/[[category]]/[slug].vue`) generates both `/:slug` (parent category empty) and `/:category/:slug` (parent category set). For URL `/world`, vue-router matches the child `/:slug` route (deeper nesting wins on tie), so `route.params` is `{ category: "", slug: "world" }` — the category name lands in `slug`, not `category`. The CategoryFilter and the parent's `category` computed both read `route.params.category` only, so they saw `""` / `undefined` and fell back to "全部" + all-signals feed. **Fix** (two lines per file): in `app/components/category-filter.vue`, `app/pages/[[category]].vue`, and `app/pages/[[category]]/[slug].vue`, changed the `category` derivation to `typeof route.params.category === 'string' && route.params.category !== '' ? route.params.category : (categorySchema.safeParse(route.params.slug ?? '').success ? route.params.slug : null)`. The child's `slugIsValid` already had the right `categorySchema.safeParse` guard, so the slideover-fetch guard was already correct — the fix was purely about reading the active category from either param slot. Verified end-to-end by direct URL navigation: `/` → 全部 active, `/tech` → 科技 active, `/finance` → 股市 active, `/world` → 國際 active. The `closeDetail()` helpers in both the parent and child page were updated to use the same dual-param derivation so closing a overlay on `/world` returns to `/world` (not `/`). `pnpm lint` / `pnpm typecheck` / `pnpm build` all pass.
- **Current-Issue Bug Fixes** (per `context/feature-specs/current-issue.md`): Three regressions reported by the user, all addressed. (1) **Category tab focus lost on first click**: `app/components/category-filter.vue` was visually correct (the `selected` computed + `text-primary` class moved to the clicked tab) but the browser's DOM focus was lost on the first click after page load — the click landed, the URL navigated, but the active tab no longer carried a focus ring. Added an explicit focus-management block: `tabRefs` is `ref<Array<HTMLButtonElement | null>>([])`, populated by a function ref `(el: Element | ComponentPublicInstance | null, idx: number) => setTabRef(el, idx)` on each `<button>`; a `watch(currentCategory, async () => { await nextTick(); tabRefs.value[idx]?.focus({ preventScroll: true }) })` re-focuses the active tab after every route change (preventScroll keeps the feed's scroll position intact). Also added `focus:outline-none focus-visible:ring-2 focus-visible:ring-brand-500 rounded-md` to the button so the focus state is visually obvious, matching the keyboard-navigation convention. (2) **Feed snapped to top when opening overlay on `/{category}`**: `app/router.options.ts:3-8` had `function parentPath(path) { ... return idx <= 0 ? '/' : path.slice(0, idx) }` — for `/finance` the `lastIndexOf('/')` is `0`, so the function returned `'/'` and the comparison `parentPath('/finance')` vs `parentPath('/finance/abc')` (which is `'/finance'`) always disagreed, triggering a scroll-to-top even when the user was just opening/closing the detail overlay on the same parent route. **Fix**: rewrote the function to take the whole route object and use `route.matched.length` — `matched.length <= 1` means "no child mounted" so the path itself is its own parent; `matched.length > 1` means a child is mounted (e.g. `[[category]]/[slug].vue`) so the parent path is everything before the last `/`. New matrix: `/` → `/`, `/finance` → `/finance`, `/abc-20260605` → `/`, `/finance/abc-20260605` → `/finance`. The `scrollBehavior` body is unchanged (just renamed `parentPath` to take a route instead of a path string). (3) **Extra `/_payload.json?_=<timestamp>` fetch when closing overlay on `/`**: traced through `node_modules/nuxt/dist/app/plugins/payload.client.js:11-15` — the `nuxt:payload` plugin's `beforeResolve` hook calls `loadPayload(to.path)` on every navigation, which delegates to `shouldLoadPayload(url)` in `app/composables/payload.js:112-126`. For URL `/`, `getRouteRules({ path: '/' })` returns `{ prerender: true }` (from `nuxt.config.ts:34-36`), `_shouldLoadPrerenderedPayload` returns `true`, and the payload is fetched — a hard reload's worth of SSR state, just to navigate within the same parent component that's already mounted. The reason `/{category}` doesn't have the same problem: no route rule there, `_isPrerenderedInManifest` returns `false`, no fetch. **Fix**: removed the `routeRules: { '/': { prerender: true } }` block from `nuxt.config.ts` and replaced it with a comment explaining why. The home page is now SSR'd on each request (a few ms slower than serving a pre-built static file on the very first visit) but the SSR payload stays inline in `window.__NUXT__` so every subsequent SPA navigation back to `/` is fetch-free. The Signal is a real-time feed (new signals added daily by the refinery pipeline) so the home page is effectively never "static" anyway — pre-rendering it would just ship a stale snapshot. Verified at the build level: `pnpm build` no longer emits `.output/public/_payload.json` or a pre-rendered `/index.html`. `pnpm lint` / `pnpm typecheck` / `pnpm build` all pass; the two pre-existing `no-console` warnings in `server/tasks/rss-ingestion.ts` and `server/utils/fetch-rss.ts` remain.
- **Current-Issue Round 2 Fixes** (per `context/feature-specs/current-issues.md`): Resolved all four remaining issues. (1) **Category tab focus lost on first click (round 2)**: the prior `tabRefs` / `setTabRef` approach at `app/components/category-filter.vue:30-37, 59` was built on the wrong assumption that Vue's function-ref signature is `(el, idx)`. Vue actually calls the ref with `(el, refs)` (`node_modules/.pnpm/@vue+runtime-core@3.5.33/.../runtime-core.cjs.js:1815-1816`), so `tabRefs.value[idx]` was always `undefined` and the `focus()` call was a silent no-op on the first click. **Fix**: rewrote as a closure factory `setTabRef(idx: number) => (el: Element | null) => …` that captures the v-for index in the outer call and returns a new ref-callback that mutates the array at the captured index. The `:ref` binding in the template is now `:ref="setTabRef(idx)"` (calls the factory, gets a fresh callback per index). The unused `ComponentPublicInstance` import was removed. **Verified** with Chrome DevTools on a hard-reloaded `/` → click "科技" → URL becomes `/tech` and `document.activeElement` is `BUTTON` with textContent `"科技"`. Click "股市" → URL becomes `/finance`, focus on `BUTTON "股市"`. The `preventScroll: true` flag on `focus()` keeps the feed's scroll position intact, so re-focusing the tab never causes an unrelated scroll jump. (2) **Feed scrolled to top on overlay open/close (round 2)**: the prior `parentPath(matched.length)` rewrite at `app/router.options.ts` still failed for `/{category}` because in Nuxt 4 the `[[category]]/[slug].vue` child route is **not** nested under `[[category]].vue` in the matched array when the child renders — both `/finance` and `/finance/abc-20260605` resolve to `matched.length === 1` (one record: the optional-dynamic parent), so the comparison `parentPath(to) === parentPath(from)` collapsed to `'/finance' === '/'` and always returned `{ top: 0, left: 0 }`. **Fix**: replaced the function with a symmetric prefix check — `return false` when `to.path.startsWith(\`${from.path}/\`) || from.path.startsWith(\`${to.path}/\`)`, which correctly handles all 6 cases: open overlay on `/{category}` (`/finance → /finance/abc`), close overlay (`/finance/abc → /finance`), swap detail (`/finance/abc → /finance/def`), root open (`/ → /abc`), and the two cross-category navigation paths (which return scroll-to-top, matching the expected behavior for a genuine page change). The `false` return suppresses both the scroll-reset and the focus-reset that Reka UI ties to navigation. `false` is symmetric — the same condition is evaluated the same way on open AND on close, so the feed's scroll position survives the full open-detail / read / close cycle. **Verified** on `/finance` at `scrollY=800`: click card → `scrollY=800` preserved + `body.overflow=hidden` (Reka UI's `useBodyScrollLock` locks the body so the user can't scroll the feed behind the overlay); press Escape → `scrollY=800` preserved + `body.overflow=visible` (lock released). The earlier transient `scrollY=79.5` reading during the open sequence was a sample taken during the overlay's slide-in animation, not a real bug; subsequent samples at the same scroll position consistently read 800. (3) **`_payload.json` re-fetch on overlay close**: confirmed via Chrome DevTools `window.fetch` instrumentation that 0 `/_payload.json` requests fire on open, close, and swap-detail. No code change needed — the `false` return from `scrollBehavior` combined with Nuxt's default keep-alive behaviour already prevents the duplicate fetch. (4) **All pages redirect to `/login`**: the prior fix at `app/middleware/auth.global.ts` was insufficient — the `@nuxtjs/supabase` v2 module **also** ships a built-in `auth-redirect` plugin (`node_modules/.pnpm/@nuxtjs+supabase@2.0.9/.../auth-redirect.js:11-39`) that registers a global route middleware running `navigateTo('/login')` whenever `useSupabaseSession()` is `null`, and it runs **before** the user's `auth.global.ts` and is not affected by any `/* … */` block in user code. **Fix**: added `supabase: { redirect: false }` to `nuxt.config.ts:7-12` to disable the module's built-in `auth-redirect` plugin at the Nuxt config level. The `DEV-AUTH-DISABLED:` marker block in the same file lists all 8 re-enable sites so the re-enable pass is greppable. **Verified** with `curl -sI` that `/`, `/login`, `/confirm`, `/finance/this-slug-does-not-exist-20260607` all return `HTTP/1.1 200 OK` with no redirect. **Side cleanup**: the `.opencode/plans/` directory was untracked but linted (19 prettier errors on the scratch plan file) — added `.opencode/**` to `eslint.config.mjs:11` and `.opencode` to `.gitignore:28` to match the existing ignore pattern for `agents/`, `context/`, `.trigger/`. `pnpm lint` / `pnpm typecheck` / `pnpm build` all pass; the only remaining warnings are the two pre-existing `no-console` warnings in `server/tasks/rss-ingestion.ts:57` and `server/utils/fetch-rss.ts:54`.

- **Feed Loading Polish** (`15-feed-loading-polish.md`): Skeleton loading states, NuxtImg optimization, UBadge tag differentiation, copy-link action, Nitro server-side caching for feed and detail, and NuxtUI toast for feedback. Created `signal-card-skeleton.vue` and `signal-detail-skeleton.vue` with `<USkeleton>`. All `<img>` replaced with `<NuxtImg>` in aspect-ratio containers with background placeholders. Tags use `<UBadge color="primary" variant="soft">`, category badges use `variant="outline"`. Signal detail layout restructured with copy-link button copying `signal/{slug}` path. Signal APIs uses Nitro's `routeRules` with 30-day TTL keyed by category/slug. Detail overlay shows skeleton during loading, `<UEmpty>` on error. Toast uses NuxtUI `useToast().add()` with responsive position (bottom-center mobile / bottom-right desktop) and stacked mode. `pnpm lint`, `pnpm typecheck`, and `pnpm build` all pass.

- **Search Command Palette** (`17-search-command-palette.md`): Added `signalSearchSchema` (`q` 1–200 chars, `limit` 1–20 default 15) to `shared/validators/signal.ts`. Created `app/components/responsive-modal.vue` — generic wrapper that renders `UModal` on desktop (≥768px) and `UDrawer` on mobile, controlled via `v-model:open`. Created `app/components/search-command-palette.vue` — `UCommandPalette` with `v-model:searchTerm`, server-side search group (`ignoreFilter: true`), mock `SignalFeed[]` items filtered client-side by title/category/tags, `onSelect` navigates via `router.push({ query: { ...route.query, signal: item.slug } })` matching `signal-card.vue` pattern. Modified `app/components/app/header.vue` — replaced non-functional `UInput` search boxes (desktop + mobile) with clickable `UButton` (search icon + "Search" + `<UKbd>⌘K</UKbd>`), registered `Cmd+K` shortcut via `defineShortcuts({ meta_k: … })`, renders `<ResponsiveModal>` containing `<SearchCommandPalette>`. `pnpm lint`, `pnpm typecheck`, and `pnpm build` all pass.

- **Search API Wiring** (`18-search-api-wiring.md`): Wired the command palette to a live backend search endpoint. Created `searchSignals()` query helper in `server/database/queries/signal.ts` — uses raw SQL via `db.execute(sql`...`)` with LEFT JOINs on `signal_tag`→`tag`, ILIKE across `title_zh`, `title_en`, `tag.name`, `array_to_string(summary_zh)`, `array_to_string(summary_en)`; GROUP BY signal.id for deduplication; tags aggregated via `json_agg(...) FILTER (WHERE tag.id IS NOT NULL)` and parsed back via `signalTagSchema.array().safeParse()`. Created `server/api/signals/search.get.ts` — validates via `signalSearchSchema`, returns `[]` for empty/whitespace `q` (graceful UX), validates response via `signalFeedSchema.array()`. Refactored `search-command-palette.vue` — removed mock data, added `useLazyFetch('/api/signals/search', { query: { q: debouncedSearchTerm, limit: 15 }, immediate: false, watch: false })`, uses `refDebounced` from VueUse for 300ms debounce, loading state only shows when `status === 'pending'` (not `idle`), clears results when search input is emptied. Exported `signalTagSchema` from `shared/validators/signal.ts`. Added GIN trigram indexes via code-first schema + auto-generated migration: `idx_signal_title_zh_trgm`, `idx_signal_title_en_trgm` in `signal.ts`; `idx_tag_name_trgm` in `tag.ts`; manually prepended `CREATE EXTENSION IF NOT EXISTS pg_trgm` to migration `0002_flawless_the_hand.sql`. Summary GIN indexes (`idx_signal_summary_*_trgm`) were removed because `array_to_string` is STABLE, not IMMUTABLE — PostgreSQL requires IMMUTABLE functions in GIN index expressions. `pnpm lint`, `pnpm typecheck`, and `pnpm build` all pass.

## In Progress

- None.

## Next Up

- **Slug-collision retries** (`-2`, `-3`, …) in `trigger/refinery-agent.ts` — see Open Questions.

## Open Questions

- **Slug uniqueness retry**: The current `refinery-agent.ts` calls `insertSignal` with the base `generateSlug(...)` output. If two articles happen to produce the same `{slugified-title-en}-{YYYYMMDD}` on the same day, the second insert will hit the `slug` unique constraint and bubble up as a task failure (Trigger.dev retries, but the retry is likely to hit the same conflict). Architecture.md mandates `-2` / `-3` / … / `-10` retry-with-suffix. Defer to a follow-up feature unit — no production impact right now (the rss-ingestion dedup by `guid` already covers the common case).
- **Postgres advisory lock or named constraint for the refinery transaction**: Currently `refinery-agent.ts` runs `db.transaction(...)` inside the Trigger.dev worker. Trigger.dev already provides run-level isolation via `ctx.run.id`, but two concurrent runs that happen to share a `guid` (very unlikely given the rss-ingestion dedup, but possible during a manual re-trigger) could in theory race. The `guid` unique constraint makes the loser fail safely. No action needed.
- **CategoryFilter is not wired to the route**: ✅ **Resolved in round-2 bug fix** — the component now reads `route.params.category` for the active highlight and calls `navigateTo` on click. **Remaining sub-question**: the 4-label UI rail (`全部` / `科技` / `股市` / `國際`) has a semantic mismatch with the API's 3-category enum (`finance` / `tech` / `world`). `國際` currently maps to `world` as a best-effort fallback; when the taxonomy expands to include a real energy category, swap the slug and (if needed) add a 5th label.

## Architecture Decisions

- **ESLint Configuration Strategy**: Use separate config objects in the flat config array to target specific file types for rules that rely on specific parsers (e.g., Vue rules). This prevents crashes when linting non-target files like Markdown.
- **Design System Configuration**: Use `app.config.ts` for color palette selection (`primary: 'brand'`, `neutral: 'neutral'`) and CSS `@theme static` block for custom color definitions with OKLCH values.
- **Layout Component Strategy**: Desktop shows header with search and avatar dropdown; mobile header collapses on scroll with hysteresis threshold, shows search bar and category filter below title.
- **Scroll Collapse Pattern**: Created generic `useScrollCollapse` composable and `AppScrollCollapseSection` wrapper component to handle scroll-based show/hide behavior. This keeps scroll logic encapsulated and reusable. CategoryFilter remains a pure presentational component without scroll awareness.
- **Objective Minimalist Typography System**: Configured a clinical, language-agnostic typography system in `ui-context.md` pairing standard readable sans-serif fonts with a monospace stack (`JetBrains Mono` / `Fira Code`). The system uses the standard Tailwind CSS size scale (`text-xs` to `text-2xl`), restricts weights to a maximum of `600` (Semi-Bold), and forbids bold text and italics in de-noised content to preserve neutrality. Enforces tabular figures (`tabular-nums`) for column alignment and specifies background-color-free entity tags for clean typography.
- **Trigger.dev Configuration**: Uses `trigger.config.ts` with `dirs: ["./trigger"]`, node-22 runtime, and retries disabled in development. Tasks are schemaTask with Zod validation for type-safe payloads. The Nitro RSS ingestion task acts as the orchestrator, triggering the Trigger.dev refinery pipeline for article processing.
- **Transactional DB Writes in the Pipeline**: All three persistence steps (insertSignal → insertTag → insertSignalTag) run inside a single `db.transaction(async (tx) => …)` inside `refinery-agent.ts`. Helpers accept an optional `DbClient` (`typeof db | transaction client`) so they remain composable. Each helper validates the incoming payload with `InsertXxxSchema.safeParse(...)` (throwing on failure) and replaces the previous `!` non-null assertion on the `.returning()` row with an explicit `if (!inserted) throw` check. Returning the inserted rows from the transaction gives the caller full IDs in one round-trip and surfaces any failure to the Trigger.dev retry path.
- **Tag Insert (Internally Upsert)**: The `insertTag` helper calls `onConflictDoUpdate({ target: tag.name, set: { name: sql`excluded.name` } })` internally. The `set` clause is a no-op (assigning `name` to its own value), and Drizzle's `$onUpdate(() => new Date())` callback does not fire on the `onConflictDoUpdate` path — so `updated_at` is never touched spuriously, and `.returning()` always yields the row (no `findTagByName` fallback needed). The function is named `insertTag` for consistency with the other insert helpers; the upsert nature is an internal implementation detail.
- **Insert Schemas Co-located with Drizzle Tables**: `drizzle-zod`'s `createInsertSchema(table).omit(...)` derives the insertable shape from the same source of truth as the table definition, so column name casing and nullability stay in lock-step. The schemas live in the same files as the table definitions (`server/database/schema/{signal,tag,signal-tag}.ts`) rather than a separate `schemas/` directory — matches `architecture.md` and `code-standards.md`, both of which prescribe a singular `schema/`.
- **Refinery Pipeline Log Vocabulary**: `trigger/refinery-agent.ts` uses a single `LOG` constants object (`refinery.start`, `refinery.extract.ok`, `refinery.refine.ok`, `refinery.slug.ok`, `refinery.mirror.{ok,skipped,err}`, `refinery.persist.ok`, `refinery.complete`) — every `logger.*` call passes a stable key as the message and dynamic data as a structured `properties` object. No string interpolation in messages, no embedding of `guid`/`pipelineRunId` in message text. The same `log(stage)` arrow spreads `{ pipelineRunId, guid, stage }` into every call so the three context fields appear on every line of the run's log timeline (and are filterable in the Trigger.dev dashboard). Failures throw `RefineryError` with a typed `code` from a fixed union — the dashboard error panel groups runs by code instead of by raw stack trace. The 5 stages stay as commented step blocks in a single linear `run()` body (no helper functions, no file split); the linear shape is preferred over decomposition because the function is the orchestrator, not a library, and a reader can scan the full pipeline top-to-bottom in one screen.
- **Cursor Index Design (`id` not `slug` as tiebreaker)**: The cursor encodes `base64({publishedAt-iso}|{id-uuid})` — `id` is the tiebreaker column in the composite index, not `slug`. Three reasons: (1) `id` is a fixed-size 16-byte UUID; `slug` is variable-length text (30–60 bytes) — smaller key columns mean a shallower B-tree and more entries per leaf page. (2) `id` is immutable; `slug` is theoretically mutable (a rename would invalidate every outstanding cursor pointing to that row). (3) `id` is the canonical internal handle; `slug` is the public URL identifier. The two identifier systems serve different audiences (machine-internal vs. human-shareable) and the cursor belongs to the internal one. Postgres B-tree's **leftmost prefix rule** means the composite `(published_at DESC, id DESC)` also serves any `ORDER BY published_at` query, which is why the legacy single-column `idx_signal_published_at` is dropped — keeping it would duplicate write overhead for zero query benefit.
- **Feed schema derived from full signal schema (no field duplication)**: `signalFeedSchema` is `signalSchema.omit({ contentEn, contentZh, summaryEn, sourceUrl })` — the field set is defined exactly once in `signalSchema`, and the lightweight feed card picks it up automatically. Adding a new field to `signalSchema` (e.g. `readingTimeMinutes`) flows through to the feed shape unless explicitly omitted; reviewers only need to audit the `omit` list when fields change. The 3-item `summaryEn` / `summaryZh` refinement is also shared as a small `summarySchema` helper inside `shared/validators/signal.ts` (used by `signalSchema`; `InsertSignal` in `server/database/schema/signal.ts` still applies the same refinement at the persistence layer via its own callback — that duplication is one line each and keeps the insert / response paths independent).
- **Cross-boundary alias bridging (Nuxt `#shared` → Trigger.dev esbuild)**: Nuxt's `#shared` alias is generated at Nuxt build time into `.nuxt/tsconfig.server.json` and is invisible to the trigger.dev CLI's esbuild bundler — so `server/database/queries/signal.ts:7` (and any other server file using `#shared/...`) fails to resolve when the trigger build follows the import chain. Considered and rejected options: (a) using the trigger config's `tsconfig` option — the schema accepts it but the bundler code in `@trigger.dev/core/dist/esm/v3/build/bundle.js:97` doesn't pass it through to esbuild, so it has no effect on module resolution (it's only used to read the file's source for indexing in `sourceFiles.js:17`); (b) installing `tsconfig-paths/register` at the top of `trigger.config.ts` — only affects the config file's own resolution, not the bundled output; (c) rewriting the offending server files to use relative paths — works but breaks the `#shared` consistency between Nitro and trigger. Chosen: a small custom esbuild plugin registered through the `build.extensions` API in `trigger.config.ts` (the `nuxt-alias-resolver` extension, ~15 lines). It registers an esbuild `onResolve` handler that matches `^#shared(?:/|$)` and returns the absolute `<root>/shared[/...].ts` path; the `.ts` suffix is appended directly because the `shared/` tree is TypeScript-only (verified — 7 files, all `.ts`). The extension lives in `trigger.config.ts` next to the `defineConfig` call so the trigger build and the alias mirror are in the same place; the comment in `trigger.config.ts` documents the mirror-with-`nuxt.config.ts` invariant. Any new `#`-prefixed alias added to `nuxt.config.ts` (e.g. `#server`) must be mirrored here — this is a known follow-up risk and is captured in the `architecture.md` stack table as a "Cross-boundary alias bridging" note.
- **No Pinia, no custom composable for the feed**: the feed page is the **direct** consumer of `useFetch` + local refs. The original `code-standards.md:104-121` documents a `useSignalFeedStore` / `useSignalDetailStore` Pinia pair, but for `#13-feed-page-wiring` the stores were deliberately not built. Two reasons: (1) invariant #9 already guarantees the `[[category]].vue` parent is mounted exactly once per browser session, so the feed's reactive state lives as long as the page — a Pinia store would be a redundant indirection layer over the parent's own refs; (2) the planned `useSignalDetailStore` is even more redundant — the child `[slug].vue` route mounts fresh per slug, so the "store surviving across navigations" benefit doesn't apply (each detail view is a clean refetch by design). The composable question was settled the same way: a wrapper would just be a thin pass-through of `useFetch` + `$fetch` that the page's `<script setup>` already calls directly in ~30 lines, and the Nuxt docs explicitly warn against shadowing the reserved `useFetch` name with a custom composable. **What this means for the existing spec**: the Pinia-store language in `code-standards.md:96-121` is now aspirational rather than enforced — any future shared state need (a 3rd consumer like the command palette, or cross-route cache) can promote the parent's refs into a `defineStore(...)` later without changing the call sites (refs read the same way inside or outside a store). The "Out of Scope" section of `12-signal-api.md:59` and the "Next Up" of the previous progress-tracker entry are now superseded. If the decision is later reversed, the migration is mechanical: lift `items` / `cursor` / `hasMore` / `isLoadingMore` into `useSignalFeedStore` state and the page reads them via `storeToRefs(...)`.
- **Query-based overlay over nested routes**: Replaced the `[[category]]/[slug].vue` child route + USlideover pattern with `?signal=slug` query param + feed-page-managed overlay. Four reasons: (1) Nuxt 4's optional-dynamic `[[category]]` and child `[slug]` create ambiguous route matching (both `/:slug` and `/:category/:slug` match single-segment paths like `/finance`), causing the category-slug desync bugs in rounds 1-2; (2) the feed's `useAsyncData` key (`signal-feed-{category}`) was stable regardless of overlay state, so opening/closing the overlay never refetched — this was correct behavior but the nested-route approach made it accidental rather than intentional; (3) Reka UI's USlideover Teleport doesn't render during SSR (onMounted never fires), so the child route's detail content was invisible to crawlers — a separate `/signal/{slug}` SSR page was always needed; (4) query params are trivially reversible via `router.replace` without creating history entries, which the "no refetch on close" invariant requires. **Overlay ownership**: `signal-detail-overlay.vue` owns the full overlay lifecycle (responsive USlideover/UDrawer via `useMediaQuery`, fetch via `useAsyncData`, close via `router.replace`); `[[category]].vue` reads `activeSlug` from `route.query.signal` and passes it down — the overlay is a feed-page concern, not a layout concern.
- **Dual DB Connection for Serverless**: Supabase's transaction pooler is required for Vercel's serverless environment (connection pooling across ephemeral functions), but some migration operations (e.g., `CREATE INDEX CONCURRENTLY`) require persistent session connections. Solution: `DATABASE_URL` uses the transaction pooler for runtime queries, `DATABASE_URL_DIRECT` uses the direct connection for Drizzle migrations. The `prepare: false` option is required because transaction poolers don't support prepared statements. Removed `max: 1` pool size constraint to allow concurrent connections in Fluid Compute.
- **ILIKE + GIN Trigram over tsvector**: Search uses `ILIKE` with GIN trigram indexes instead of PostgreSQL full-text search (`tsvector` / `plainto_tsquery`). Three reasons: (1) trigram search handles partial matches and CJK text without language-specific configuration; (2) `tsvector` requires language-aware stemming which adds complexity for mixed zh/en content; (3) the dataset size (< 100k signals) makes trigram indexes performant without the setup overhead. The `pg_trgm` extension is created via a manually-prepended line in the Drizzle-generated migration (`0002_flawless_the_hand.sql`), while the GIN index definitions live in the schema files (`signal.ts`, `tag.ts`) and are auto-generated by `pnpm db:generate`. Title and tag columns have GIN trigram indexes; summary array columns use ILIKE without indexes (sequential scan) because `array_to_string` is STABLE, not IMMUTABLE — PostgreSQL requires IMMUTABLE functions in GIN index expressions.

## Session Notes

- ESLint is now fully functional and passing.
- Design system is configured with brand color as primary, dark-only theme, black background, and 0.375rem border radius.
- Layout components implemented: AppHeader (desktop/mobile responsive), CategoryFilter (horizontal scroll), MobileBottomNavbar (mobile-only fixed bottom nav).
- All components use NuxtUI semantic color tokens (`border-default`, `text-highlighted`, etc.) and OKLCH brand color system.
- Typography system fully documented and unified with the design-system context using standard Tailwind default classes and background-color-free entity tags.
- Updated app to follow Typography guidelines: `--font-mono` added to main.css for monospace stack; layout components use appropriate font scales and weights per the "Objective Minimalist" design philosophy.
- Drizzle ORM fully configured with Supabase dual connection (transaction pooler for runtime, direct for migrations). `server/database/` is server-only (never imported from `app/`). All 3 tables created in Supabase with proper indexes, enums, and foreign keys. CamelCase → snake_case mapping handled by Drizzle's `casing: 'snake_case'` option. `prepare: false` set for transaction pooler compatibility.
- Fixed `TypeError: fetch failed` / `HeadersOverflowError` when fetching Yahoo Finance articles: `@extractus/article-extractor`'s built-in `retrieve()` uses Node's native undici fetch with a 16KB header limit, which Yahoo Finance exceeds. Solved by using `undici` directly in `shared/utils/extractor.ts` with `setGlobalDispatcher(new Agent({ maxHeaderSize: 32768 }))` to configure a 32KB header limit globally. Uses browser User-Agent headers to bypass anti-bot measures.
- **Drizzle enum diff side-effect of module-private `categoryEnum`**: Because spec #10 made `categoryEnum` a module-private constant (not re-exported from `server/database/schema/index.ts`), Drizzle's snapshot diff never sees the enum in the schema namespace — the snapshot's `enums` field stays `{}` instead of `{ "public.category": { … } }`. This breaks generated migrations in **both directions**:
  - **Incremental migration on an existing DB**: Drizzle sees the enum in the old snapshot but not the new one, so it emits a spurious `DROP TYPE "public"."category"` line at the end of the migration. The `signal.category` column still depends on the type — leaving the line in would fail the migration or destroy the type and break all rows. **Fix**: manually strip the `DROP TYPE` line before running `pnpm db:migrate`.
  - **Fresh migration on an empty DB**: Drizzle never sees the enum, so it skips the `CREATE TYPE "public.category" AS ENUM('finance', 'tech', 'world')` line entirely. The first `CREATE TABLE signal` would then fail with `type "category" does not exist`. **Fix**: manually prepend the `CREATE TYPE` line to the migration before running `pnpm db:migrate`.
  - This is a known trade-off of the spec #10 design decision (the module-private refactor is required to satisfy `PostgresJsTransaction`'s `TablesRelationalConfig` constraint). Document the fix in code review and add it to the migration checklist going forward. A future refactor could move `categoryEnum` to a dedicated `server/database/schema/enums.ts` file and re-export it from `schema/index.ts` — Drizzle's namespace scan would then pick it up automatically and the manual fix disappears, at the cost of the relational-config constraint needing a different workaround.
- **Clean-slate reset (development only)**: Dropped the Supabase schema (Drizzle-managed: `signal`, `tag`, `signal_tag` tables, `public.category` enum, `drizzle.__drizzle_migrations` journal) via a one-off `tsx` script that ran `DROP SCHEMA … CASCADE` + per-table `DROP TABLE … CASCADE` + `DROP TYPE … CASCADE`. Deleted `server/database/migrations/` entirely, then ran `pnpm db:generate` to produce a single fresh `0000_deep_catseye.sql` and `pnpm db:migrate` to apply it. Verified the resulting state via `pg_indexes` / `information_schema.tables` / `pg_enum`: 3 tables, 1 enum, 5 signal indexes (legacy `idx_signal_category_published` + composite `idx_signal_published_at_id` + 3 per-category partials). The previous 5 dev signals were intentionally lost — this was a dev-only reset, not a destructive change to production data.

- **DB Connection Update for Serverless** (`16-update-db-connection.md`): Configured dual database connections for Vercel's serverless environment. Updated `.env` and `.env.example` — `DATABASE_URL` now uses the transaction pooler connection string (for runtime queries), added `DATABASE_URL_DIRECT` with the direct connection string (for migrations requiring persistent sessions like `CREATE INDEX CONCURRENTLY`). Added `DATABASE_URL_DIRECT` to `shared/env.ts` for env var validation. Updated `drizzle.config.ts` to use `DATABASE_URL_DIRECT` for migrations. Updated `server/database/index.ts` — disabled `prepare` statements (transaction pooler doesn't support them) and removed `max: 1` pool size constraint to avoid harming concurrency in Fluid Compute.
